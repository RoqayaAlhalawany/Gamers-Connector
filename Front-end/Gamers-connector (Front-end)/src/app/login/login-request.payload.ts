/**
 * Interface representing the payload for a login request.
 */
export interface LoginRequestPayload {
  usernameOrEmail: string; // Represents the username or email entered by the user
  password: string; // Represents the password entered by the user
}
