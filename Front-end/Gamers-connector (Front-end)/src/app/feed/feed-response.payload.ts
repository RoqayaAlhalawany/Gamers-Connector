export interface feedResponsePayload {
  text: string; // Represents the text content of a feed response
  images: any[]; // Represents an array of images associated with the feed response
  videos: any[]; // Represents an array of videos associated with the feed response
}
